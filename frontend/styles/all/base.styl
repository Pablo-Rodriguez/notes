@import './normalize.styl'
@import './fonts.styl'

* {
  margin 0px
  padding 0px
}

img {
  vertical-align top
}

body {
  font-size 16px
  font-family 'Open Sans', sans-serif
  overflow hidden
}

a
a:visited
a:active
a:hover
  color inherit
  text-decoration none

